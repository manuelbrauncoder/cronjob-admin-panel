import { HandleKeyPipe } from './handle-key.pipe';

describe('HandleKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new HandleKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
